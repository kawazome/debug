from setuptools import setup, find_packages

setup(
    name='debug',
    version="1.0.0",
    description="log output module for debugging",
    long_description="",
    author='kawazome',
    packages=find_packages()
)